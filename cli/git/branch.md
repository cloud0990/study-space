~~~ bash
# 브렌치 조회
$ git branch

# 브렌치 생성
$ git branch <신규 branch>
$ git branch <신규 branch> <분기 branch>
# ex: git branch newbranch master

# 브렌치 삭제
$ git branch -d <삭제 branch>

# 브렌치명 수정
$ git branch -m <기존 branch> <신규 branch>
~~~

~~~ bash
# 체크아웃
$ git checkout <branch>
~~~